# d:\2025
记录的是每日的功能内容，所得和一些tips，不要在里面放置大型工程
此仓库由脚本初始化，包含当前工作区的文件。

说明：
- 本地 Git 用户名和邮箱已为提交设置为 `Repo User <repo@example.com>`（可修改为你自己的信息）。
- 若需推送到远程，请先在远程创建仓库并运行 `git remote add origin <url>` 然后 `git push -u origin main`（或 `master`，依据远程默认分支）。

如果想修改提交作者信息：

```powershell
Set-Location -Path 'D:\2025'
git config user.name "你的姓名"
git config user.email "you@example.com"
```
